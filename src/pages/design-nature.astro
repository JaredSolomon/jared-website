---
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Jared's Hub | Serenity</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,600;1,400&family=Nunito:wght@300;600&display=swap" rel="stylesheet">
    <style>
      :root {
        --bg-color: #0a0a0a;
        --text-primary: #ffffff;
        --text-secondary: #a1a1aa;
        --accent: #10b981;
        --card-bg: rgba(255, 255, 255, 0.03);
        --card-bg: rgba(255, 255, 255, 0.03);
      }

      body {
        margin: 0;
        font-family: 'Nunito', sans-serif;
        background-color: var(--bg-color);
        color: var(--text-primary);
        height: 100vh;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        overflow: hidden;
      }

      .bg-grid {
        position: fixed;
        top: -10%;
        left: -10%;
        width: 120%;
        height: 120%;
        background-image: radial-gradient(rgba(255, 255, 255, 0.1) 1px, transparent 1px);
        background-size: 30px 30px;
        z-index: -1;
        transform: 
          translate(
            calc(var(--move-x, 0) * -30px), 
            calc(var(--move-y, 0) * -30px)
          ) 
          scale(calc(1 + max(var(--move-x, 0), var(--move-y, 0)) * 0.05));
        transition: transform 0.2s cubic-bezier(0.2, 0.8, 0.2, 1);
      }

      main {
        text-align: center;
        z-index: 1;
        position: relative;
      }

      h1 {
        font-family: 'Lora', serif;
        font-size: 4.5rem;
        font-weight: 400;
        font-style: italic;
        margin: 0 0 1.5rem 0;
        color: var(--text-primary);
        opacity: 0.9;
      }

      p {
        color: var(--text-secondary);
        font-size: 1.25rem;
        margin-bottom: 4rem;
        font-weight: 300;
        letter-spacing: 0.5px;
      }

      .grid {
        display: flex;
        gap: 2.5rem;
        justify-content: center;
      }

      .card {
        background: var(--card-bg);
        border-radius: 24px;
        padding: 2.5rem;
        text-decoration: none;
        color: var(--text-primary);
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1rem;
        border: 1px solid rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(10px);
      }

      .card:hover {
        transform: translateY(-8px);
        background: rgba(255, 255, 255, 0.06);
        border-color: rgba(255, 255, 255, 0.1);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
      }

      .card-icon {
        font-size: 2.5rem;
        margin-bottom: 0.5rem;
        background: rgba(255, 255, 255, 0.05);
        width: 80px;
        height: 80px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        color: var(--text-primary);
      }

      .card h2 {
        margin: 0;
        font-family: 'Lora', serif;
        font-size: 1.4rem;
        font-weight: 600;
      }

      .card span {
        color: var(--text-secondary);
        font-size: 0.9rem;
      }

      .back-link {
        position: absolute;
        top: 2rem;
        left: 2rem;
        color: var(--text-secondary);
        text-decoration: none;
        font-weight: 600;
        transition: color 0.2s;
        font-size: 0.9rem;
      }

      .back-link:hover {
        color: var(--text-primary);
      }
    </style>
  </head>
  <body>
    <div class="bg-grid"></div>
    <a href="/" class="back-link">← Back Home</a>
    
    <main>
      <h1>Serenity</h1>
      <p>Focus on what matters.</p>
      
      <div class="grid">
        <a href="/games" class="card">
          <div class="card-icon">✨</div>
          <h2>Games Hub</h2>
          <span>Daily Rituals</span>
        </a>
      </div>
    </main>

    <script>
      document.addEventListener('mousemove', (e) => {
        const x = (e.clientX / window.innerWidth) - 0.5;
        const y = (e.clientY / window.innerHeight) - 0.5;
        document.body.style.setProperty('--move-x', x.toString());
        document.body.style.setProperty('--move-y', y.toString());
      });
    </script>


  </body>
</html>
